#!/usr/bin/env bash

# Install Joy Pkg to read the controller
sudo apt install ros-melodic-joy

# Intall Twist Joy Pkg to transform controller data to twist msg
sudo apt install ros-melodic-teleop-twist-joy
